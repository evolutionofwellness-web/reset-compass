/* app.js (stable) — v127 marker only (no behavioral changes) */
(function(){
  'use strict';
  window.APP_VERSION = window.APP_VERSION || 'v127';
  // existing code remains unchanged — this file will be the same as the repo's current stable app.js
  // (when committing, we will push the unchanged app.js content with the version marker included)
})();